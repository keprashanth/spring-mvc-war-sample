
# Spring MVC WAR Sample

## Build
```bash
mvn clean package
```

Deploy `target/spring-mvc-war-1.0.0.war` to your servlet container (e.g., Tomcat).

Then open: `http://<host>:<port>/spring-mvc-war-1.0.0/hello`
